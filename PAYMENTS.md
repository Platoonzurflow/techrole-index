# Платежи TechRole Index

Актуальность сравнения и live-проверки: 24 июля 2026 года. Это техническая памятка, а не юридическая или налоговая консультация. Публичные тарифы могут измениться; договор и тариф в личном кабинете провайдера имеют приоритет. Владелец подтвердил статус самозанятого на НПД, действующую связь Robokassa с «Мой налог» и активные «Робочеки СМЗ».

## Принятое техническое решение

Основной кандидат для подтверждённого статуса НПД — **Robokassa**, резервный — **ЮKassa**.

Robokassa стала основной после подтверждения НПД: её официальная страница прямо поддерживает самозанятых без ИП и бесплатную автоматическую отправку чеков через «Робочеки СМЗ». ЮKassa технически зрелее и на публичном тарифе дешевле для части карточных платежей, но с 29 декабря 2025 года не автоматизирует чеки самозанятых; их пришлось бы вручную создавать в «Мой налог».

Production работает с `PAYMENTS_PROVIDER=robokassa` и `PAYMENTS_MODE=live`: официальный sandbox прошёл checkout, ResultURL, повтор уведомления и выдачу Premium без списания денег, а контрольный live-платёж подтвердил реальное списание и entitlement. Реализованы также локальный demo и резервный адаптер ЮKassa. Robokassa формирует подписанный server-side checkout, подписывает номенклатуру чека, проверяет ResultURL по Паролю №2, отвечает `OK<InvId>`, дедуплицирует повторы, поддерживает live refund API по Password3 и сверяет pending-состояние.

## Сравнение официальных вариантов

| Провайдер | Кому подходит по публичным материалам | Публичная комиссия | Карты / СБП / регулярные | Чеки и возвраты | API, webhook, тестирование | Вывод |
| --- | --- | --- | --- | --- | --- | --- |
| [ЮKassa](https://yookassa.ru/) | Юрлица, ИП, самозанятые | Базовый тариф для части карточных способов — 3,5%; точные ставки и СБП определяет договор. С 2026 года к части комиссий добавляется НДС 22% от комиссии | Карты, СБП, сохранение способа оплаты/автоплатежи | ИП/компания: онлайн-касса или [Чеки от ЮKassa](https://yookassa.ru/developers/payment-acceptance/receipts/54fz/basics). Самозанятый: вручную в «Мой налог»; [автоматизация ЮKassa недоступна](https://yookassa.ru/developers/payment-acceptance/receipts/basics). Полные/частичные возвраты по API | [Тестовый магазин](https://yookassa.ru/developers/payment-acceptance/testing-and-going-live/testing) до договора, API idempotency, HTTPS webhook; подлинность проверяется [авторизованным GET объекта](https://yookassa.ru/developers/using-api/webhooks) | Резерв для НПД; сильный API, но чеки придётся делать вручную |
| [CloudPayments](https://cloudpayments.ru/) | Зарегистрированный бизнес; публичная документация не даёт достаточно ясного подтверждения работы только со статусом НПД, это нужно письменно уточнить до выбора | На главной указывается «примерно от 2,6%», итог индивидуален; в 2026 учитывается НДС на комиссию | Карты, СБП, рекуррентные платежи | CloudKassir/receipt object, API возвратов | Сильная [документация API](https://developers.cloudpayments.ru/), тестовая эмуляция; уведомления подписаны HMAC и повторяются | Хорошая API-альтернатива для ИП/ООО после подтверждения условий и статуса |
| [Robokassa](https://robokassa.com/) | Самозанятые, ИП, компании | Калькулятор для НПД при минимальном обороте 22.07.2026 показывает стартовый тариф: карты РФ 3,9%, СБП 2,7%; итог определяет договор | Карты, СБП, 15+ способов; рекуррентные платежи требуют предварительного согласования | Бесплатные автоматические [Робочеки СМЗ](https://robokassa.com/solutions/business/samozanyatye/); live refund API использует Password3 и OpKey | [IsTest=1 и отдельные тестовые пароли](https://docs.robokassa.ru/ru/testing-mode), подпись ResultURL, обязательный `OK<InvId>`, [refund API](https://docs.robokassa.ru/ru/refund-api/) | Основной вариант для подтверждённого НПД |
| [Т‑Касса](https://www.tbank.ru/business/online-payments/internet-acquiring/) | По публичной инструкции — ИП, ООО/другие организации; не простой НПД без ИП | Индивидуальная, без абонентской платы | Карты, СБП, рекуррентные сценарии | Сторонняя/интегрированная касса, возвраты | Token-проверка [уведомлений](https://developer.tbank.ru/eacq/intro/developer/notification), повторы, тестирование | Реальная дополнительная альтернатива для ИП/компании, особенно при экосистеме Т‑Банка |
| [Stripe](https://stripe.com/global) | Россия отсутствует в официальном списке поддерживаемых стран | Не применимо | Не применимо для российского получателя | Не применимо | Нельзя строить запуск на фиктивной иностранной регистрации | Не рассматривается для владельца из РФ без реально существующей допустимой иностранной структуры |

Официальные страницы тарифов: [ЮKassa](https://yookassa.ru/docs/support/payments/fees), [Robokassa](https://robokassa.com/payments/tarify/). Для CloudPayments и Т‑Кассы итоговую ставку нужно получить письменно в оферте/договоре; нельзя считать рекламное «от» гарантированной ставкой.

## Что владелец обязан сделать лично

1. Страна получателя — Россия, статус — самозанятый НПД. До live остаётся проверить, что продаваемая услуга разрешена НПД, годовой доход не превышает действующий лимит и нет запрещённых посреднических/агентских отношений.
2. Самостоятельно зарегистрировать этот статус и открыть личный кабинет провайдера. Самозанятому отдельный расчётный счёт по НПД сам по себе не обязателен; ИП/компании обычно указывают расчётный счёт для выплат. Конкретное требование проверяется в договоре провайдера.
3. Пройти KYC и заключить договор: предоставить ИНН, паспорт и селфи/идентификацию; для ИП — ОГРНИП/данные ЕГРИП и при необходимости лицензии/доверенность; для компании — ОГРН, уставные и банковские реквизиты, сведения о руководителе, бенефициарах и полномочиях. Точный список формирует провайдер.
4. Для основного варианта подключить в Robokassa «Робочеки СМЗ» и проверить привязку «Мой налог». Сайт передаёт только server-side позицию `service`, полную оплату и `tax=none`; правильность предмета расчёта и наименования нужно подтвердить до live. При резервной ЮKassa чек создаётся вручную в «Мой налог» и передаётся покупателю. Возврат также должен быть отражён в НПД.
5. Заполнить реквизиты продавца, продукт, цену, правила оказания услуги, возвраты, сроки хранения данных и контакты в шаблонах `/legal/*`; перед live передать их юристу. Проверить уведомление Роскомнадзора, локализацию персональных данных и договоры с хостингом, провайдером, кассой/ОФД и почтой.
6. Получить постоянный HTTPS-домен и круглосуточный hosting. Tailscale Funnel подходит для теста, но зависит от включённого ПК; при выключении webhook не будет принят вовремя.
7. В личном кабинете Robokassa создать магазин и тестовые Пароль №1/№2, выбрать тот же алгоритм подписи, что в `.env`, и настроить ResultURL. Для live отдельно создать Password3 для возвратов. Не присылать пароль от кабинета, SMS-коды, данные Госуслуг, полный номер карты или CVC.
8. Указать ResultURL `https://<постоянный-домен>/api/v1/payments/webhooks/robokassa` методом POST и проверить ответ `OK<InvId>`. Success/Fail URL передаются сервером в каждом заказе. Для резервной ЮKassa webhook остаётся `https://<домен>/api/v1/payments/webhooks/yookassa`.
9. Явно подтвердить включение production-списаний после полного теста. Без этого `PAYMENTS_MODE=live` не включается.

Нельзя выполнить вместо владельца: зарегистрировать НПД/ИП/компанию, открыть банковский счёт, пройти паспортный KYC/селфи, подписать договор или оферту, подтвердить SMS/КЭП/Госуслуги, выбрать налоговый режим и ставку НДС, зарегистрировать кассу в ФНС, дать юридическое заключение, принять договор провайдера и дать согласие на реальные списания.

## Что уже реализовано

- расширяемый серверный registry разрешённых SKU; сейчас реально продаётся только `premium_30_days`, а браузер передаёт только product code;
- неизменяемый snapshot названия, цены, результата услуги, entitlement, номенклатуры чека и refund policy в каждом заказе;
- `payment_orders`, `payment_refunds`, версии принятой оферты и аудит sanitized payment events;
- client/provider idempotency и защита от повторных webhook;
- строгая сверка order id, payment id, суммы, валюты и test/live режима;
- HMAC-аутентификация локального demo webhook;
- для ЮKassa — Basic auth к официальному API, `Idempotence-Key` и повторный авторизованный GET для проверки webhook вместо доверия присланному JSON;
- для Robokassa — уникальный числовой `InvId`, подписи Паролем №1/№2, allowlisted hash algorithm, server-side `Shp_order_id`, проверка суммы/режима, form-urlencoded ResultURL и обязательный plain-text ack;
- server-side Robokassa Receipt для услуги НПД; карточные данные и произвольная номенклатура браузера не принимаются;
- переход в Robokassa выполняется HTML-формой `POST`, как требует документация для платежей с `Receipt`; action ограничен официальным HTTPS-доменом провайдера;
- канонический action формы — `https://auth.robokassa.ru/Merchant/Index.aspx`; он закреплён в runtime guard и не может быть переопределён произвольным внешним адресом в live;
- live refund API Robokassa подписывается HS256/Password3, получает OpKey только через подписанный OpStateExt и сверяется фоновой задачей каждые пять минут; официальный OpStateExt не поддерживает тестовые операции, поэтому refund-сценарий без денег проверяется в demo и mock-контракте;
- только серверное предоставление Premium после `succeeded`, терминальные отмены и отзыв/пересчёт цепочки продлений после полного возврата;
- owner-scoped история заказов/возвратов без provider id, credentials и платёжных реквизитов;
- server-side receipt item для ЮKassa из продукта и email пользователя, когда выбран режим кассы; ставка НДС задаётся только после юридического подтверждения;
- страницы sandbox checkout, ожидания, успеха и ошибки; статус успеха читается с сервера;
- шаблоны оферты, возвратов, политики данных и согласий с явными незаполненными полями;
- backend, provider, component и Playwright тесты, включая неверную подпись, подмену суммы, повтор, отмену и возврат.

Карточные реквизиты не принимаются и не хранятся приложением. Для реального сценария пользователь переходит на HTTPS-страницу провайдера.

## Локальный sandbox

В `.env`:

```dotenv
PAYMENTS_ENABLED=true
PAYMENTS_PROVIDER=demo
PAYMENTS_MODE=test
PAYMENTS_LIVE_CONFIRMED=false
PAYMENTS_LEGAL_APPROVED=false
PAYMENTS_STABLE_HTTPS_CONFIRMED=false
PAYMENTS_TERMS_VERSION=draft-2026-07-22
PAYMENTS_SELLER_STATUS=self_employed
PAYMENTS_FISCALIZATION_MODE=disabled
PREMIUM_30_DAYS_PRICE_RUB=1.00
```

Затем:

```powershell
docker compose run --rm migrate
docker compose up -d backend frontend
```

Зарегистрируйте новый аккаунт, откройте `/account`, примите тестовые условия и нажмите «Тестовая оплата». На локальной странице нет полей карты; кнопки только имитируют успешный или отменённый результат.

## Тестовый магазин Robokassa — основной путь

Test shop использует отдельные тестовые credentials:

```dotenv
PAYMENTS_ENABLED=true
PAYMENTS_PROVIDER=robokassa
PAYMENTS_MODE=test
PAYMENTS_LIVE_CONFIRMED=false
PAYMENTS_LEGAL_APPROVED=false
PAYMENTS_SELLER_STATUS=self_employed
PAYMENTS_FISCALIZATION_MODE=disabled
ROBOKASSA_MERCHANT_LOGIN=<test MerchantLogin>
ROBOKASSA_TEST_PASSWORD1=<test Password #1>
ROBOKASSA_TEST_PASSWORD2=<test Password #2>
ROBOKASSA_HASH_ALGORITHM=sha256
```

Алгоритм в технических настройках магазина обязан совпадать с `ROBOKASSA_HASH_ALGORITHM`. Для проверки автоматического чека после подключения тестовой фискализации заменить mode на `robokassa`; номенклатуру и сумму всё равно формирует сервер. Password3 в тестовом контуре не нужен: официальный OpStateExt работает только в основном режиме.

## Тестовый магазин ЮKassa — резерв

Владелец создаёт test shop и локально добавляет:

```dotenv
PAYMENTS_ENABLED=true
PAYMENTS_PROVIDER=yookassa
PAYMENTS_MODE=test
PAYMENTS_LIVE_CONFIRMED=false
PAYMENTS_LEGAL_APPROVED=false
YOOKASSA_SHOP_ID=<test shopId>
YOOKASSA_SECRET_KEY=<test secret key>
```

Для sandbox можно оставить `PAYMENTS_FISCALIZATION_MODE=disabled`, если тестовый магазин не подключён к тестовой кассе. Если проверяются чеки, выбрать договорной режим и тестовый `YOOKASSA_VAT_CODE` после подтверждения бухгалтера. Секреты остаются только в `.env`/secret store, никогда в Git, issue, логе или browser bundle.

## Guard для production

Live-конфигурация намеренно не стартует, пока одновременно не выполнены все условия:

- `PAYMENTS_ENABLED=true`, `PAYMENTS_PROVIDER=robokassa`, `PAYMENTS_MODE=live`;
- `PAYMENTS_STABLE_HTTPS_CONFIRMED=true` только после проверки постоянного домена, TLS и круглосуточного host;
- production `ROBOKASSA_MERCHANT_LOGIN`, `ROBOKASSA_LIVE_PASSWORD1`, `ROBOKASSA_LIVE_PASSWORD2` и `ROBOKASSA_LIVE_PASSWORD3` только в secret store; live никогда не использует legacy/test aliases;
- `PAYMENTS_LIVE_CONFIRMED=true` после явного решения владельца;
- `PAYMENTS_LEGAL_APPROVED=true` и версия оферты без префикса `draft-`;
- подтверждён `PAYMENTS_SELLER_STATUS`;
- для НПД выбран `PAYMENTS_FISCALIZATION_MODE=robokassa`, в Robokassa и связанном приложении «Мой налог» виден активный «Робочеки СМЗ», после чего вручную установлен `PAYMENTS_ROBOCHEKI_SMZ_CONFIRMED=true`;
- `ROBOKASSA_HASH_ALGORITHM` совпадает с техническими настройками магазина;
- все Robokassa payment/refund URL остаются официальными значениями из `.env.example`;
- `PUBLIC_BASE_URL` и `FRONTEND_ORIGIN` указывают на один постоянный HTTPS origin.

Перед переключением автоматически повторить sandbox, неверную подпись, подмену суммы/SKU, повтор webhook, pending, cancel, success, refund, reconciliation и entitlement. После включения live владелец самостоятельно оплачивает контрольные 290 ₽ своей картой/СБП; агент не вводит платёжные данные. Затем проверить заказ, webhook, чек «Мой налог»/Robokassa, срок Premium и поступление. До этой проверки обычный пользовательский live-трафик не открывается.

### Результат контрольного live-платежа 24 июля 2026 года

Перед переключением создан backup PostgreSQL, все автоматические gates и production health прошли. Владелец самостоятельно выполнил оплату; агент не видел и не вводил платёжные данные. Первые счета № 30–32 остались в `State=10` без списаний и ResultURL из-за недостаточного остатка на карте, что владелец уточнил у поддержки. Они честно остаются `pending`: сайт не выдумывает terminal status провайдера.

После пополнения контрольный счёт № 33 на 290 ₽ успешно прошёл 3-D Secure. `OpStateExt` подтвердил расчётное состояние, подписанный ResultURL обработан, событие `payment.succeeded` сохранено один раз, заказ получил `succeeded`, а отдельный entitlement Premium выдан на 30 дней — до 23 августа 2026 года. Владелец увидел фискальный чек и в Robokassa, и в «Мой налог»; production оставлен в `PAYMENTS_MODE=live`. Письма в ожидаемом внешнем ящике не было, потому что email платёжного аккаунта отличается: checkout намеренно передаёт Robokassa server-side email авторизованного пользователя, а не произвольный адрес из браузера. Персональные адреса в документации и событиях платежей не публикуются.

Текущий реализованный продукт — разовый доступ Premium на 30 дней за 290 ₽, без автопродления. «Личное ведение» остаётся заявкой с отдельно согласуемыми объёмом и ценой и не создаёт автоматический payment order. Добавление нового SKU требует полной server-side карточки, fulfillment и legal/receipt contract; пользовательская сумма из браузера запрещена.

## Проверка готовности без раскрытия секретов

После входа администратором откройте `/admin`. Раздел «Готовность Robokassa» показывает два независимых checklist: тестовый магазин и реальные платежи. ResultURL можно скопировать в технические настройки магазина.

`GET /api/v1/admin/payment-readiness` возвращает только названия проверок и `ready: true/false`. MerchantLogin, Пароли №1/№2/№3 и ключи других провайдеров не сериализуются. Endpoint закрыт admin-auth; live остаётся заблокированным, пока не выполнены все legal, fiscalization, hosting и owner-confirmation guards.
